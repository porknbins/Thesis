amodel:
  lstm:
    sequence_length: 24
    features: 1
    epochs: 50
    batch_size: 32
  svm:
    kernel: rbf
    C: 1.0
    epsilon: 0.1

federated:
  num_clients: 3
  rounds: 10
  aggregation: fedavg

data:
  train_split: 0.8
  validation_split: 0.1
